# homepage

Static links page for jeffdt.com, pointing to personal projects: rolomux,
boomerang, teleport, rastermaster, and homskillet discography.

Plain HTML/CSS, no build step. Served via GitHub Pages from `main`.
